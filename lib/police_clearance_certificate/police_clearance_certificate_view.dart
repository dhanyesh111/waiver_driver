import 'package:flutter/material.dart';

class PoliceClearanceCertificateScreen extends StatelessWidget {
  const PoliceClearanceCertificateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
