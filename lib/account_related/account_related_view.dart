import 'package:flutter/material.dart';

class AccountRelatedScreen extends StatelessWidget {
  const AccountRelatedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
