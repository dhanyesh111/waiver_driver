import 'package:flutter/material.dart';

class VehicleProofScreen extends StatelessWidget {
  const VehicleProofScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
