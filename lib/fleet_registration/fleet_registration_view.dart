import 'package:flutter/material.dart';

class FleetRegistrationScreen extends StatelessWidget {
  const FleetRegistrationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
